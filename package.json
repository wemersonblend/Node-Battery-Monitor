{
  "version": "0.1.0",
  "private": false,
  "dependencies": {
    "node-battery": "^0.2.2",
    "node-schedule": "^0.1.15",
    "notify-send": "^0.1.2",
    "winston": "^0.8.3"
  },
  "devDependencies": {
    "pkg": "^4.3.1"
  },
  "scripts" : {
  	"start": "node app.js",
  	"build": "pkg app.js --out-path build --options expose-gc"
  }
}
